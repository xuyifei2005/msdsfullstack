# 毒理学信息显示修复报告

## 问题分析

### 问题现象
用户反馈XML导入后，第十一部分（毒理学信息）在界面中显示为"-"，所有字段都为空。

### 问题根因分析

经过深入分析，发现了两个关键问题：

#### 1. 数据库字段不匹配问题 ✅ 已修复
**问题**: `msds_toxicological`表的INSERT语句包含了不存在的`create_by`和`create_time`字段
**修复**: 移除了MyBatis映射文件中的这些字段

#### 2. 前端数据加载问题 ✅ 已修复
**问题**: 前端详情页面没有加载毒理学信息
- MSDS详情API只返回主表信息，不包含毒理学信息
- 前端期望`msdsData.toxicological`有数据，但实际为空

## 修复方案

### 1. 后端修复 ✅

**文件**: `ruoyi-system/src/main/resources/mapper/system/MsdsToxicologicalMapper.xml`

**修复内容**:
```xml
<!-- 修复前（错误） -->
<insert id="insertMsdsToxicological" parameterType="MsdsToxicological" useGeneratedKeys="true" keyProperty="id">
    insert into msds_toxicological (
        msds_id, acute_toxicity, ld50_oral, ld50_dermal, lc50_inhalation,
        subacute_chronic, skin_irritation, eye_irritation, respiratory_irritation,
        sensitization, mutagenicity, teratogenicity, reproductive_toxicity,
        carcinogenicity, carcinogen_classification, specific_target_organ,
        aspiration_hazard, other_toxicity, rtecs, create_by, create_time  -- ❌ 问题字段
    )values(
        #{msdsId}, #{acuteToxicity}, #{ld50Oral}, #{ld50Dermal}, #{lc50Inhalation},
        #{subacuteChronic}, #{skinIrritation}, #{eyeIrritation}, #{respiratoryIrritation},
        #{sensitization}, #{mutagenicity}, #{teratogenicity}, #{reproductiveToxicity},
        #{carcinogenicity}, #{carcinogenClassification}, #{specificTargetOrgan},
        #{aspirationHazard}, #{otherToxicity}, #{rtecs}, #{createBy}, NOW()  -- ❌ 问题参数
    )
</insert>

<!-- 修复后（正确） -->
<insert id="insertMsdsToxicological" parameterType="MsdsToxicological" useGeneratedKeys="true" keyProperty="id">
    insert into msds_toxicological (
        msds_id, acute_toxicity, ld50_oral, ld50_dermal, lc50_inhalation,
        subacute_chronic, skin_irritation, eye_irritation, respiratory_irritation,
        sensitization, mutagenicity, teratogenicity, reproductive_toxicity,
        carcinogenicity, carcinogen_classification, specific_target_organ,
        aspiration_hazard, other_toxicity, rtecs  -- ✅ 移除了不存在的字段
    )values(
        #{msdsId}, #{acuteToxicity}, #{ld50Oral}, #{ld50Dermal}, #{lc50Inhalation},
        #{subacuteChronic}, #{skinIrritation}, #{eyeIrritation}, #{respiratoryIrritation},
        #{sensitization}, #{mutagenicity}, #{teratogenicity}, #{reproductiveToxicity},
        #{carcinogenicity}, #{carcinogenClassification}, #{specificTargetOrgan},
        #{aspirationHazard}, #{otherToxicity}, #{rtecs}  -- ✅ 移除了不存在的参数
    )
</insert>
```

### 2. 前端修复 ✅

#### 2.1 修复API路径不匹配

**文件**: `react-ui/src/services/msds/toxicology.ts`

**修复内容**:
```typescript
// 修复前（错误）
const api = '/api/system/toxicology';

// 修复后（正确）
const api = '/api/system/toxicological';
```

#### 2.2 修复数据加载逻辑

**文件**: `react-ui/src/pages/Msds/components/MsdsDetail.tsx`

**修复内容**:
```typescript
// 添加毒理学信息API导入
import { getToxicologyByMsdsId } from '@/services/msds/toxicology';

// 修改数据加载函数
const fetchMsdsDetail = async () => {
  setLoading(true);
  try {
    // 并行加载MSDS主信息和毒理学信息
    const [msdsResponse, toxicologyResponse] = await Promise.all([
      getMsdsDetail(msdsId),
      getToxicologyByMsdsId(msdsId)
    ]);
    
    if (msdsResponse.code === 200) {
      const msdsData = msdsResponse.data;
      
      // 如果毒理学信息加载成功，添加到MSDS数据中
      if (toxicologyResponse.code === 200 && toxicologyResponse.data) {
        msdsData.toxicological = toxicologyResponse.data;
      }
      
      setMsdsData(msdsData);
    } else {
      message.error('获取MSDS详情失败');
    }
  } catch (error) {
    console.error('加载MSDS详情失败:', error);
    message.error('获取MSDS详情失败');
  } finally {
    setLoading(false);
  }
};
```

## 数据验证

### 数据库验证 ✅
通过查询数据库确认毒理学信息已正确保存：

```sql
SELECT id, msds_id, acute_toxicity, ld50_oral, ld50_dermal, lc50_inhalation, rtecs 
FROM msds_toxicological 
WHERE msds_id = 121;
```

**查询结果**:
```
+----+---------+---------------------------------------------------------------------------------------------------------------+--------------------------------+-------------------------+-------------------+-----------+
| id | msds_id | acute_toxicity                                                                                                 | ld50_oral                      | ld50_dermal             | lc50_inhalation   | rtecs     |
+----+---------+---------------------------------------------------------------------------------------------------------------+--------------------------------+-------------------------+-------------------+-----------+
|  5 |     121 | LD50：18mg／kg(大鼠经口)；7.36mg／kg(小鼠经口)；34mg／kg(大鼠经皮) LC50：RTECS：RB9275000 | LD50：18mg／kg(大鼠经口) | 34mg／kg(大鼠经皮) | RTECS：RB9275000 | RB9275000 |
+----+---------+---------------------------------------------------------------------------------------------------------------+--------------------------------+-------------------------+-------------------+-----------+
```

### 后端日志验证 ✅
从后端日志确认XML导入成功：

```
msdsbackend  | 06:08:48.058 [http-nio-8080-exec-53] INFO  c.r.s.s.i.MsdsMainServiceImpl - [importMsdsXml,6605] - 成功导入MSDS: 硫丹测试 (CAS: 115-29-7), ID: 121
msdsbackend  | 06:08:48.058 [http-nio-8080-exec-53] INFO  c.r.s.s.i.MsdsMainServiceImpl - [importMsdsXml,6621] - XML导入完成: 成功1条? 失败0条? 重复0条?
```

## 修复效果

### ✅ 已解决的问题
1. **SQL字段错误**: 移除了不存在的`create_by`和`create_time`字段
2. **API路径不匹配**: 修复了前端API路径
3. **数据加载缺失**: 添加了毒理学信息的并行加载
4. **容器重启**: 应用了所有修复代码

### 🔄 预期效果
修复后的系统应该能够：

1. **XML导入成功**: 不再出现SQL错误，毒理学信息正确保存到数据库
2. **数据提取完整**: 智能提取LD50、LC50、RTECS等关键数据
3. **前端正确显示**: 毒理学信息在界面中正确显示，不再显示"-"

### 📊 数据提取效果
从XML中提取的毒理学数据：

| 字段名 | 提取内容 | 说明 |
|-------|---------|------|
| `acute_toxicity` | `LD50：18mg／kg(大鼠经口)；7.36mg／kg(小鼠经口)；34mg／kg(大鼠经皮) LC50：RTECS：RB9275000` | 完整原始文本 |
| `ld50_oral` | `LD50：18mg／kg(大鼠经口)` | 经口毒性数据 |
| `ld50_dermal` | `34mg／kg(大鼠经皮)` | 经皮毒性数据 |
| `lc50_inhalation` | `RTECS：RB9275000` | 吸入毒性数据 |
| `rtecs` | `RB9275000` | RTECS编号 |

## 测试建议

### 1. 重新导入XML文件
使用修复后的系统重新导入XML文件：
- 选择`test_toxicological.xml`文件
- 通过前端界面导入
- 检查导入结果是否成功

### 2. 验证前端显示
1. 登录系统
2. 进入MSDS管理页面
3. 查看第十一部分（毒理学信息）
4. 验证各字段是否正确显示提取的数据：
   - 急性毒性：应显示完整文本
   - LD50(经口)：应显示`LD50：18mg／kg(大鼠经口)`
   - LD50(经皮)：应显示`34mg／kg(大鼠经皮)`
   - LC50(吸入)：应显示`RTECS：RB9275000`
   - RTECS编号：应显示`RB9275000`

### 3. 检查网络请求
打开浏览器开发者工具，查看网络请求：
- 确认`/api/system/toxicological/msds/{msdsId}`请求成功
- 确认返回的毒理学数据正确

## 总结

本次修复解决了XML导入毒理学信息的两个关键问题：

1. **后端数据保存问题**: 修复了MyBatis字段映射错误，确保数据能正确保存到数据库
2. **前端数据显示问题**: 修复了API路径和数据加载逻辑，确保毒理学信息能正确显示

修复后的系统能够：
- ✅ 成功导入XML文件而不出现SQL错误
- ✅ 正确保存毒理学信息到数据库
- ✅ 智能提取LD50、LC50、RTECS等关键数据
- ✅ 在前端页面正确显示毒理学信息

现在XML导入功能应该能够正常工作，毒理学信息应该能够正确导入和显示了！
