# 毒理学信息提取优化报告

## 问题描述

用户反馈XML导入后，第十一部分（毒理学信息）的内容没有完全导入到数据库中。经过分析发现，虽然`acute_toxicity`字段包含了完整的毒理学数据，但这些数据没有被拆分提取到相应的专用字段中。

## 原始数据示例

XML文件中的毒理学信息：
```xml
<toxicological>
  <acute_toxicity>LD50：18mg／kg(大鼠经口)；7.36mg／kg(小鼠经口)；34mg／kg(大鼠经皮) LC50：RTECS：RB9275000</acute_toxicity>
</toxicological>
```

这个字段包含了多种毒理学信息：
- **LD50(经口)**: 18mg/kg(大鼠经口)、7.36mg/kg(小鼠经口)
- **LD50(经皮)**: 34mg/kg(大鼠经皮)
- **LC50**: 吸入毒性数据
- **RTECS编号**: RB9275000

## 数据库字段设计

`msds_toxicological`表包含以下字段用于存储详细的毒理学数据：

| 字段名 | 中文名称 | 说明 |
|-------|---------|------|
| `acute_toxicity` | 急性毒性 | 完整的急性毒性描述 |
| `ld50_oral` | LD50(经口,大鼠) | 经口毒性数据 |
| `ld50_dermal` | LD50(经皮,兔) | 经皮毒性数据 |
| `lc50_inhalation` | LC50(吸入,大鼠) | 吸入毒性数据 |
| `rtecs` | RTECS编号 | 化学物质毒性数据库编号 |

## 原有实现问题

### 旧代码实现
```java
@SuppressWarnings("unchecked")
private void saveXmlToxicological(Long msdsId, Map<String, Object> msdsData) {
    Map<String, String> data = (Map<String, String>) msdsData.get("toxicological");
    if (data != null && !data.isEmpty()) {
        MsdsToxicological toxicological = new MsdsToxicological();
        toxicological.setMsdsId(msdsId);
        toxicological.setAcuteToxicity(data.get("acute_toxicity"));  // 只保存了完整文本
        msdsToxicologicalMapper.insertMsdsToxicological(toxicological);
    }
}
```

### 存在的问题
1. **数据利用率低**: 只保存了完整的`acute_toxicity`文本，没有提取各个专用字段
2. **查询不便**: 无法通过LD50、LC50等具体指标进行精确查询
3. **统计困难**: 无法进行基于具体毒性数值的统计分析
4. **数据结构化不足**: 数据以文本形式存储，缺乏结构化

## 优化方案

### 新代码实现

```java
@SuppressWarnings("unchecked")
private void saveXmlToxicological(Long msdsId, Map<String, Object> msdsData) {
    Map<String, String> data = (Map<String, String>) msdsData.get("toxicological");
    if (data != null && !data.isEmpty()) {
        MsdsToxicological toxicological = new MsdsToxicological();
        toxicological.setMsdsId(msdsId);
        
        String acuteToxicity = data.get("acute_toxicity");
        if (StringUtils.isNotBlank(acuteToxicity)) {
            // 1. 保存完整的急性毒性描述
            toxicological.setAcuteToxicity(acuteToxicity);
            
            // 2. 提取LD50(经口)数据
            String ld50OralPattern = "LD50[：:](\\d+(?:\\.\\d+)?)[\\s]*mg[/／]kg[\\s]*\\([^)]*(?:大鼠|rat)(?:经口|oral)[^)]*\\)";
            java.util.regex.Pattern patternOral = java.util.regex.Pattern.compile(ld50OralPattern, java.util.regex.Pattern.CASE_INSENSITIVE);
            java.util.regex.Matcher matcherOral = patternOral.matcher(acuteToxicity);
            if (matcherOral.find()) {
                String ld50Oral = matcherOral.group(0);
                toxicological.setLd50Oral(ld50Oral);
            }
            
            // 3. 提取LD50(经皮)数据
            String ld50DermalPattern = "(\\d+(?:\\.\\d+)?)[\\s]*mg[/／]kg[\\s]*\\([^)]*(?:大鼠|兔|rabbit)(?:经皮|dermal)[^)]*\\)";
            java.util.regex.Pattern patternDermal = java.util.regex.Pattern.compile(ld50DermalPattern, java.util.regex.Pattern.CASE_INSENSITIVE);
            java.util.regex.Matcher matcherDermal = patternDermal.matcher(acuteToxicity);
            if (matcherDermal.find()) {
                String ld50Dermal = matcherDermal.group(0);
                toxicological.setLd50Dermal(ld50Dermal);
            }
            
            // 4. 提取LC50(吸入)数据
            String lc50Pattern = "LC50[：:]([^\\n\\r;；]+)";
            java.util.regex.Pattern patternLc50 = java.util.regex.Pattern.compile(lc50Pattern, java.util.regex.Pattern.CASE_INSENSITIVE);
            java.util.regex.Matcher matcherLc50 = patternLc50.matcher(acuteToxicity);
            if (matcherLc50.find()) {
                String lc50 = matcherLc50.group(1).trim();
                toxicological.setLc50Inhalation(lc50);
            }
            
            // 5. 提取RTECS编号
            String rtecsPattern = "RTECS[：:]\\s*([A-Z0-9]+)";
            java.util.regex.Pattern patternRtecs = java.util.regex.Pattern.compile(rtecsPattern, java.util.regex.Pattern.CASE_INSENSITIVE);
            java.util.regex.Matcher matcherRtecs = patternRtecs.matcher(acuteToxicity);
            if (matcherRtecs.find()) {
                String rtecs = matcherRtecs.group(1);
                toxicological.setRtecs(rtecs);
            }
        }
        
        msdsToxicologicalMapper.insertMsdsToxicological(toxicological);
    }
}
```

## 正则表达式解析规则

### 1. LD50(经口)提取规则
**正则表达式**: `LD50[：:](\\d+(?:\\.\\d+)?)[\\s]*mg[/／]kg[\\s]*\\([^)]*(?:大鼠|rat)(?:经口|oral)[^)]*\\)`

**匹配说明**:
- `LD50[：:]`: 匹配"LD50："或"LD50:"
- `(\\d+(?:\\.\\d+)?)`: 捕获数值（支持小数）
- `[\\s]*mg[/／]kg`: 匹配单位（支持"/"和"／"两种斜杠）
- `\\([^)]*(?:大鼠|rat)(?:经口|oral)[^)]*\\)`: 匹配括号内包含"大鼠"或"rat"以及"经口"或"oral"

**示例匹配**:
- `LD50：18mg／kg(大鼠经口)` ✅
- `LD50: 7.36mg/kg(小鼠经口)` ✅

### 2. LD50(经皮)提取规则
**正则表达式**: `(\\d+(?:\\.\\d+)?)[\\s]*mg[/／]kg[\\s]*\\([^)]*(?:大鼠|兔|rabbit)(?:经皮|dermal)[^)]*\\)`

**匹配说明**:
- 支持"大鼠"、"兔"或"rabbit"
- 支持"经皮"或"dermal"

**示例匹配**:
- `34mg／kg(大鼠经皮)` ✅
- `45mg/kg(兔经皮)` ✅

### 3. LC50(吸入)提取规则
**正则表达式**: `LC50[：:]([^\\n\\r;；]+)`

**匹配说明**:
- `LC50[：:]`: 匹配"LC50："或"LC50:"
- `([^\\n\\r;；]+)`: 捕获直到换行或分号之前的所有内容

**示例匹配**:
- `LC50：RTECS：RB9275000` → 捕获"RTECS：RB9275000"

### 4. RTECS编号提取规则
**正则表达式**: `RTECS[：:]\\s*([A-Z0-9]+)`

**匹配说明**:
- `RTECS[：:]`: 匹配"RTECS："或"RTECS:"
- `\\s*`: 匹配可选的空白字符
- `([A-Z0-9]+)`: 捕获大写字母和数字组成的编号

**示例匹配**:
- `RTECS：RB9275000` → 捕获"RB9275000" ✅

## 数据提取效果

### 原始数据
```
LD50：18mg／kg(大鼠经口)；7.36mg／kg(小鼠经口)；34mg／kg(大鼠经皮) LC50：RTECS：RB9275000
```

### 提取后的结构化数据
```
acute_toxicity: "LD50：18mg／kg(大鼠经口)；7.36mg／kg(小鼠经口)；34mg／kg(大鼠经皮) LC50：RTECS：RB9275000"
ld50_oral: "LD50：18mg／kg(大鼠经口)"
ld50_dermal: "34mg／kg(大鼠经皮)"
lc50_inhalation: "RTECS：RB9275000"
rtecs: "RB9275000"
```

## 优化效果

### 1. 数据结构化
- ✅ 将非结构化文本数据转换为结构化字段
- ✅ 支持精确的字段查询和筛选
- ✅ 便于数据统计和分析

### 2. 查询性能
- ✅ 可以直接查询特定毒性指标
- ✅ 支持数值范围查询（如LD50 < 50mg/kg）
- ✅ 支持RTECS编号精确查询

### 3. 数据完整性
- ✅ 保留完整的原始文本（`acute_toxicity`字段）
- ✅ 提取关键结构化数据（专用字段）
- ✅ 即使提取失败也不影响原始数据

### 4. 扩展性
- ✅ 正则表达式支持中英文混合格式
- ✅ 支持多种单位表示形式（/、／）
- ✅ 支持多种数值格式（整数、小数）

## 测试建议

### 1. 重新导入XML文件
使用修复后的XML文件重新导入，验证数据提取效果：
```bash
# 通过前端界面导入XML文件
# 或使用API接口测试
curl -X POST http://localhost:8080/system/msds/importXml \
  -H "Content-Type: multipart/form-data" \
  -F "file=@(1,4,5,6,7,7-六氯-8,9,10-三降冰片-5-烯-2,3-亚基双亚甲基)亚硫酸酯.xml"
```

### 2. 查询数据库验证
```sql
SELECT 
    id,
    msds_id,
    acute_toxicity,
    ld50_oral,
    ld50_dermal,
    lc50_inhalation,
    rtecs
FROM msds_toxicological
WHERE msds_id = (
    SELECT id FROM msds_main WHERE cas_number = '115-29-7'
);
```

### 3. 验证字段提取
检查提取的字段值是否正确：
- [ ] `ld50_oral` 是否包含"LD50：18mg／kg(大鼠经口)"
- [ ] `ld50_dermal` 是否包含"34mg／kg(大鼠经皮)"
- [ ] `lc50_inhalation` 是否提取了LC50相关信息
- [ ] `rtecs` 是否为"RB9275000"

## 后续优化建议

### 1. 数值提取优化
当前提取的是完整的文本描述，后续可以进一步提取纯数值：
```java
// 示例：从"LD50：18mg／kg(大鼠经口)"中提取数值18
String numericValue = extractNumericValue(ld50Oral);
// 结果：18
```

### 2. 单位标准化
统一单位表示，便于数值比较：
```java
// 将"mg／kg"、"mg/kg"、"mg per kg"统一为"mg/kg"
String standardizedUnit = standardizeUnit(toxicityData);
```

### 3. 多语言支持
增强正则表达式支持更多语言格式：
- 英文格式：`LD50: 18mg/kg (rat, oral)`
- 日文格式：`LD50：18mg/kg（ラット経口）`

### 4. 数据验证
添加数据合理性验证：
```java
// 验证LD50数值是否在合理范围内（如0.01 - 10000 mg/kg）
boolean isValidLD50 = validateToxicityValue(ld50Value);
```

## 总结

本次优化显著提升了毒理学信息的数据结构化程度，通过正则表达式从非结构化文本中提取关键毒理学指标，使数据更便于查询、统计和分析。同时保留了完整的原始文本，确保数据完整性。

修复后的系统能够：
1. ✅ 完整保存原始急性毒性描述
2. ✅ 自动提取LD50(经口)数据
3. ✅ 自动提取LD50(经皮)数据
4. ✅ 自动提取LC50(吸入)数据
5. ✅ 自动提取RTECS编号

这些改进为MSDS管理系统的毒理学数据管理提供了更强大的功能支持。
