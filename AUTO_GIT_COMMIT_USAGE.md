# MSDS项目自动Git提交和推送脚本使用说明

## 概述

本脚本用于自动化MSDS项目的Git提交和推送流程，简化日常开发中的版本控制操作。脚本会自动检测未提交的更改，添加到暂存区，生成提交消息，并推送到远程仓库。

## 脚本功能

1. **自动检测Git状态** - 检查当前目录是否为Git仓库
2. **智能提交消息生成** - 根据更改文件自动生成有意义的提交消息
3. **一键提交推送** - 自动完成从添加、提交到推送的全过程
4. **参数化控制** - 支持自定义提交消息、强制推送等选项
5. **安全保护机制** - 包含多种检查和错误处理机制
6. **详细日志记录** - 记录操作过程和结果到日志文件

## 使用方法

### 基本使用

```powershell
# 在项目根目录执行，自动完成提交和推送
.\auto-git-commit.ps1
```

### 带参数使用

```powershell
# 指定提交消息
.\auto-git-commit.ps1 -CommitMessage "更新用户登录功能"

# 强制推送（即使有冲突）
.\auto-git-commit.ps1 -Force

# 模拟运行（不实际执行操作）
.\auto-git-commit.ps1 -DryRun

# 显示帮助信息
.\auto-git-commit.ps1 -Help
```

### 组合参数使用

```powershell
# 指定提交消息并强制推送
.\auto-git-commit.ps1 -CommitMessage "紧急修复" -Force

# 模拟运行带自定义消息
.\auto-git-commit.ps1 -DryRun -CommitMessage "测试提交"
```

## 参数说明

| 参数 | 类型 | 描述 |
|------|------|------|
| `-CommitMessage` | String | 自定义提交消息，如果不指定则自动生成 |
| `-Force` | Switch | 强制推送模式，即使存在冲突也会继续 |
| `-DryRun` | Switch | 模拟运行模式，只显示将要执行的操作，不实际执行 |
| `-Help` | Switch | 显示帮助信息 |

## 自动生成的提交消息格式

脚本会根据检测到的文件更改自动生成提交消息，格式如下：

```
自动提交: 新增X个文件 修改Y个文件 删除Z个文件 (YYYY-MM-DD HH:MM)
```

例如：
```
自动提交: 新增2个文件 修改1个文件 (2023-05-15 14:30)
```

## 使用场景示例

### 场景1：日常开发提交

开发完功能后，在项目根目录执行：
```powershell
.\auto-git-commit.ps1
```

脚本会自动：
1. 检查Git状态
2. 添加所有更改文件到暂存区
3. 生成描述性提交消息
4. 提交到本地仓库
5. 拉取远程更新
6. 推送更改到远程仓库

### 场景2：紧急修复提交

对于紧急修复，可以指定提交消息：
```powershell
.\auto-git-commit.ps1 -CommitMessage "紧急修复登录验证问题"
```

### 场景3：冲突处理

当遇到推送冲突时，可以使用强制推送：
```powershell
.\auto-git-commit.ps1 -Force
```

### 场景4：安全检查

在执行前想确认将要执行的操作：
```powershell
.\auto-git-commit.ps1 -DryRun
```

## 注意事项

1. **执行目录**：必须在Git仓库根目录执行此脚本
2. **权限要求**：确保具有Git仓库的读写权限
3. **网络连接**：推送操作需要网络连接到远程仓库
4. **Git配置**：确保已经配置好Git用户信息和远程仓库
5. **冲突处理**：如果远程仓库有更新，脚本会先尝试拉取，如有冲突需要手动解决

## 错误处理

脚本包含完善的错误处理机制：
- Git未安装检测
- 非Git仓库目录检测
- 文件操作权限检测
- 网络连接异常处理
- 冲突检测和处理建议

当出现错误时，脚本会：
1. 显示清晰的错误信息
2. 记录错误到日志文件
3. 终止执行并返回错误码

## 日志文件

脚本会在当前目录生成 `auto-git-log.txt` 日志文件，记录每次执行的详细信息，包括：
- 执行时间
- 操作步骤
- 文件列表
- 执行结果
- 错误信息（如果有）

## 常见问题

### Q: 如何查看脚本的帮助信息？
A: 执行 `.\auto-git-commit.ps1 -Help`

### Q: 脚本是否会自动处理冲突？
A: 脚本会尝试拉取远程更新，但复杂的冲突仍需手动处理

### Q: 如何自定义提交消息？
A: 使用 `-CommitMessage` 参数，例如：`.\auto-git-commit.ps1 -CommitMessage "自定义消息"`

### Q: 如何在不实际执行的情况下测试脚本？
A: 使用 `-DryRun` 参数，例如：`.\auto-git-commit.ps1 -DryRun`

## 版本信息

- **版本**: 1.0
- **作者**: AI Assistant
- **创建日期**: 2023年

## 更新日志

- v1.0 (2023): 初始版本，实现基本的自动提交和推送功能