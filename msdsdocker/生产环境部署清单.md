# MSDS系统生产环境部署清单 📋

## 🎯 部署目标
- **服务器IP**: 39.107.211.72
- **域名**: flymsds.cn
- **部署目录**: /opt/msds

---

## 📦 需要传输到服务器的文件

### 1. Docker配置文件
```
源路径: D:\XUYIFEI\XUPROJECTS\msdsfullstack\msdsdocker\
目标路径: /opt/msds/msdsdocker/

需要传输的文件：
├── docker-compose.prod.yml     # 生产环境容器编排文件
├── .env.prod                   # 生产环境变量文件
├── nginx/
│   ├── nginx.prod.conf         # Nginx生产配置
│   ├── ssl/                    # SSL证书目录
│   │   ├── flymsds.cn.pem     # SSL证书文件
│   │   └── flymsds.cn.key     # SSL私钥文件
│   └── html/                   # 前端静态文件目录
├── mysql/
│   └── conf.d/                 # MySQL配置文件
├── msdsdatabaseinitdb/         # 数据库初始化脚本
├── deploy.sh                   # 部署脚本
└── backup-restore.sh           # 备份恢复脚本
```

### 2. 前端构建产物
```
源路径: D:\XUYIFEI\XUPROJECTS\msdsfullstack\msdsPC\ruoyi-MsdsPc-react\react-ui\dist\
目标路径: /opt/msds/msdsdocker/nginx/html/

包含文件：
├── index.html                  # 主页面
├── static/                     # 静态资源
│   ├── css/                   # 样式文件
│   ├── js/                    # JavaScript文件
│   └── media/                 # 媒体文件
└── favicon.ico                # 网站图标
```

### 3. 后端JAR文件（已在容器中构建）
```
容器内路径: /app/ruoyi-admin/target/ruoyi-admin.jar
说明: 后端JAR文件已在msdsbackend容器中构建完成，无需单独传输
```
- 本地文件挂载到容器的所以直接从这个文件复制即可！
/opt/msds/backend-build
scp -r msdsPC\ruoyi-MsdsPc-react\ruoyi-admin\target\ruoyi-admin.jar root@39.107.211.72:/opt/msds/backend-build/

-- 这个编译在docker目录在【msdsPC\ruoyi-MsdsPc-react\ruoyi-admin】
scp ../msdsPC/ruoyi-MsdsPc-react/ruoyi-admin/target/ruoyi-admin.jar root@39.107.211.72:/opt/msds/backend-build/



## 🖥️ 后台服务器生产环境运行命令（服务器端）

### 1. 启动服务
```bash
# 1. 进入部署目录
cd /opt/msds/msdsdocker

# 2. 启动所有服务
docker-compose -f docker-compose.prod.yml up -d

# 3. 等待服务启动
sleep 30

# 4. 检查容器状态
docker-compose -f docker-compose.prod.yml ps
```








### 4. SSL证书文件
```
源路径: 阿里云下载的证书包
目标路径: /opt/msds/msdsdocker/nginx/ssl/

文件：
├── flymsds.cn.pem             # SSL证书
└── flymsds.cn.key             # SSL私钥
```

---

## 🚀 部署执行命令

### 1. 本地准备命令（在Windows开发机上执行）

```powershell
# 1. 构建前端
cd D:\XUYIFEI\XUPROJECTS\msdsfullstack\msdsPC\ruoyi-MsdsPc-react\react-ui
npm run build:prod

# 2. 上传Docker配置文件
cd D:\XUYIFEI\XUPROJECTS\msdsfullstack
scp -r msdsdocker root@39.107.211.72:/opt/msds/

# 3. 上传前端构建产物
scp -r msdsPC\ruoyi-MsdsPc-react\react-ui\dist\* root@39.107.211.72:/opt/msds/msdsdocker/nginx/html/

# 4. 上传SSL证书（如果本地有证书文件）
scp flymsds.cn.pem root@39.107.211.72:/opt/msds/msdsdocker/nginx/ssl/
scp flymsds.cn.key root@39.107.211.72:/opt/msds/msdsdocker/nginx/ssl/
```

### 2. 服务器部署命令（在Linux服务器上执行）

```bash
# 1. SSH连接到服务器
ssh root@39.107.211.72

# 2. 进入部署目录
cd /opt/msds/msdsdocker

# 3. 设置文件权限
chmod +x deploy.sh backup-restore.sh
chmod 600 nginx/ssl/flymsds.cn.key
chmod 644 nginx/ssl/flymsds.cn.pem

# 4. 检查环境变量配置
cat .env.prod

# 5. 执行一键部署
./deploy.sh

# 6. 查看容器状态
docker-compose -f docker-compose.prod.yml ps

# 7. 查看日志（如有问题）
docker-compose -f docker-compose.prod.yml logs -f
```

---

## ✅ 部署验证清单

### 1. 容器状态检查
```bash
# 检查所有容器是否正常运行
docker ps

# 应该看到以下容器：
# - msdsmysql (healthy)
# - msdsredis (healthy) 
# - msdsbackend (healthy)
# - msdsfrontend (healthy)
# - msdsnginx (healthy)
```

### 2. 网络连通性检查
```bash
# 检查端口监听
netstat -tlnp | grep -E ':(80|443|3306|6379|8080)'

# 检查防火墙状态
ufw status
```

### 3. 应用功能检查
```bash
# 1. 访问前端页面
curl -I http://flymsds.cn
curl -I https://flymsds.cn

# 2. 检查API接口
curl -I http://flymsds.cn/prod-api/system/user/profile

# 3. 检查数据库连接
docker exec msdsmysql mysql -u msds_user -p -e "SELECT 1"
```

### 4. SSL证书检查
```bash
# 检查证书有效性
openssl x509 -in /opt/msds/msdsdocker/nginx/ssl/flymsds.cn.pem -text -noout

# 在线检查SSL
# 访问: https://www.ssllabs.com/ssltest/analyze.html?d=flymsds.cn
```

---

## 🔧 常用维护命令

### 容器管理
```bash
# 查看容器状态
docker-compose -f docker-compose.prod.yml ps

# 重启所有服务
docker-compose -f docker-compose.prod.yml restart

# 重启单个服务
docker-compose -f docker-compose.prod.yml restart msdsbackend

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f [service_name]

# 进入容器
docker exec -it msdsbackend bash
docker exec -it msdsmysql mysql -u root -p
```

### 数据库管理
```bash
# 备份数据库
./backup-restore.sh backup

# 恢复数据库
./backup-restore.sh restore backup_filename.sql

# 查看数据库状态
docker exec msdsmysql mysqladmin -u root -p status
```

### 应用更新
```bash
# 更新代码
git pull origin main

# 重新构建并部署
./deploy.sh

# 仅重启应用（不重新构建）
docker-compose -f docker-compose.prod.yml restart msdsbackend msdsfrontend
```

---

## 🚨 故障排查

### 常见问题及解决方案

1. **容器启动失败**
   ```bash
   # 查看详细错误信息
   docker-compose -f docker-compose.prod.yml logs [service_name]
   
   # 检查配置文件
   docker-compose -f docker-compose.prod.yml config
   ```

2. **数据库连接失败**
   ```bash
   # 检查数据库容器状态
   docker exec msdsmysql mysqladmin -u root -p ping
   
   # 检查网络连接
   docker exec msdsbackend ping msdsmysql
   ```

3. **SSL证书问题**
   ```bash
   # 检查证书文件
   ls -la /opt/msds/msdsdocker/nginx/ssl/
   
   # 验证证书
   openssl x509 -in nginx/ssl/flymsds.cn.pem -text -noout
   ```

4. **前端页面无法访问**
   ```bash
   # 检查Nginx配置
   docker exec msdsnginx nginx -t
   
   # 检查静态文件
   ls -la /opt/msds/msdsdocker/nginx/html/
   ```

---

## 📞 技术支持

如遇到部署问题，请按以下步骤收集信息：

1. 容器状态：`docker ps -a`
2. 服务日志：`docker-compose -f docker-compose.prod.yml logs`
3. 系统资源：`free -h && df -h`
4. 网络状态：`netstat -tlnp`

---

**部署完成后，您的MSDS实验室管理系统将在 https://flymsds.cn 上线！** 🎉