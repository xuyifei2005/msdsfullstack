services:
  # 后端服务
  backend:
    image: eclipse-temurin:17-jdk
    volumes:
      - ../../msdsPC/ruoyi-MsdsPc-react:/app
      - ../../msdsPC/ruoyi-MsdsPc-react/uploadPath:/app/uploadPath
    working_dir: /app
    command: |
      sh -c "echo 'Starting development environment setup...' && 
      apt-get update && apt-get install -y openssh-server bash file dos2unix curl wget git && 
      echo 'Installing Node.js 18...' && 
      curl -fsSL https://deb.nodesource.com/setup_18.x | bash - && 
      apt-get install -y nodejs && 
      echo 'Installing Maven...' && 
      apt-get install -y maven && 
      echo 'Verifying installations...' && 
      java -version && 
      node -v && 
      npm -v && 
      mvn -version && 
      mkdir -p /run/sshd && 
      ssh-keygen -A && 
      sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config && 
      sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config && 
      echo 'root:msds_dev_password' | chpasswd && 
      /usr/sbin/sshd && 
      echo 'SSH service started successfully' && 
      echo 'Development environment ready!' && 
      tail -f /dev/null"
    ports:
      - "8080:8080"
      - "5005:5005"
      - "2222:22"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/msds_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=msds_user
      - SPRING_DATASOURCE_PASSWORD=msds_dev_password
    depends_on:
      - mysql
      - redis
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 前端服务
  frontend:
    image: node:18
    volumes:
      - ../../msdsPC/ruoyi-MsdsPc-react/react-ui:/app
    working_dir: /app
    # 执行 npm start 命令启动前端应用，并指定监听所有可用网络接口
    # command: npm start -- --host 0.0.0.0
    command: |
      sh -c "
      echo 'Setting up frontend development environment...' &&
      apt-get update &&
      apt-get install -y openssh-server curl wget git &&
      echo 'Installing additional development tools...' &&
      apt-get install -y build-essential &&
      mkdir -p /run/sshd &&
      echo 'root:msds_dev_password' | chpasswd &&
      sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config &&
      sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config &&
      ssh-keygen -A &&
      /usr/sbin/sshd &&
      echo 'Frontend development environment ready!' &&
      tail -f /dev/null"
    ports:
      - "3000:3000"
      - "2223:22"
    environment:
      - REACT_APP_API_URL=http://backend:8080
    depends_on:
      - backend

  # MySQL数据库
  mysql:
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: msds_dev
      MYSQL_USER: msds_user
      MYSQL_PASSWORD: msds_dev_password
      MYSQL_ROOT_PASSWORD: root_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ../../docker/msdsfullstatckcompose/msdsdatabaseinitdb:/docker-entrypoint-initdb.d

  # Redis缓存
  redis:
    image: redis:7
    ports:
      - "1379:6379"
    volumes:
      - redis_data:/data

volumes:
  mysql_data:
  redis_data: